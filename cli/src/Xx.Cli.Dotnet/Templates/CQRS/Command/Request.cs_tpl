using Annium.Architecture.ViewModel;
using {{ command_namespace }};

namespace {{ request_namespace }};

public class {{ name }}Request : IRequest<{{ name }}Command>
{
    {{~ for $field in request_fields ~}}
    public {{ $field.item1 }} {{ $field.item2 }} { get; set; }
    {{~ end ~}}
}