using System.Net;
using System.Threading.Tasks;
using Annium.AspNetCore.IntegrationTesting;
using Annium.Net.Http;
using Annium.Testing;

namespace {{name}}
{
    public class IndexControllerTest : IntegrationTest
    {
        protected IRequest http => GetRequest<Startup>(
            builder => builder.UseServicePack<TestServicePack>()
        );

        [Fact]
        public async Task True_IsTrue()
        {
            // act
            var response = await http.Get("/").RunAsync();

            // assert
            response.StatusCode.IsEqual(HttpStatusCode.OK);
        }
    }
}